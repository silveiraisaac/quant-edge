# Server-only. Never prefix provider credentials with NEXT_PUBLIC_.
KITE_API_KEY=
KITE_ACCESS_TOKEN=
# Map current licensed cash-equity instruments from the official instrument master.
# Example shape only; replace token with the verified provider token:
KITE_INSTRUMENTS_JSON=[]

# Optional Supabase PostgreSQL + Auth (configured in phase 8)
SUPABASE_URL=
SUPABASE_ANON_KEY=
